# Plan 8.3 Summary: Rollover Mejorado & Plantillas

## Changes
- **Rollover.gs**:
    - Enhanced `rolloverToNextYear()`:
        - Now only archives tasks that are `Terminado` or `Cancelado`.
        - Added a pre-archive summary alert showing task counts and affected projects.
        - Integrated `autoUpdateProjectStatuses_()` to refresh project states post-rollover.
- **Config.gs**:
    - Added `SHEET_TEMPLATES` and `HEADERS_TEMPLATES`.
- **Main.gs**:
    - Updated `initStructure()` to support the `TEMPLATES` sheet with protection and example data.
    - Added `applyTemplate()`: automates project instantiation from predefined task sets in the `TEMPLATES` sheet.
    - Added "📋 Aplicar plantilla" to the main menu.

## Verification Results
- Rollover correctly filters tasks by status and date.
- Templates are correctly applied, calculating relative dates from today.
- Automated status engine triggers after template application and rollover.
