---
phase: 7
plan: 3
wave: 2
---

# Plan 7.3: Auto-Sync & Documentation

## Objective
Wire the dashboard and views to auto-refresh when the Gantt view is refreshed, and update project documentation to reflect the new Phase 7 features.

## Context
- Gantt.gs (refreshGanttView — auto-sync point)
- README.md (user documentation)
- .gsd/SPEC.md (success criteria)

## Tasks

<task type="auto">
  <name>Auto-sync Dashboard from refreshGanttView</name>
  <files>Gantt.gs</files>
  <action>
    1. In `refreshGanttView`, after the `refreshTimelineData()` call, add `refreshDashboard()`.
       - This ensures DASHBOARD stays current when users click "Refrescar vista Gantt".
    2. Do NOT auto-sync VIEWS — those are on-demand snapshots triggered by the user.

    - Keep refreshGanttView lean: the dashboard computation is fast (no complex queries, just counting).
  </action>
  <verify>grep "refreshDashboard" Gantt.gs | Select-String "refreshGanttView" -Quiet</verify>
  <done>`refreshGanttView` calls `refreshDashboard()` after `refreshTimelineData()`.</done>
</task>

<task type="auto">
  <name>Update documentation</name>
  <files>README.md, .gsd/SPEC.md</files>
  <action>
    1. Update `README.md`:
       - Add DASHBOARD and VIEWS sheets to the sheet reference section.
       - Document KPIs provided (completion %, overdue count, status breakdown).
       - Document the "Vistas" submenu with viewByProject/viewByResponsible options.

    2. Update `.gsd/SPEC.md`:
       - Mark TIMELINE_DATA success criterion as completed (Phase 6 done).
       - Add new success criteria for Phase 7:
         - [ ] Dashboard KPIs computed from live data
         - [ ] Filtered views by project and responsible

  </action>
  <verify>grep "DASHBOARD" README.md && grep "Dashboard" .gsd/SPEC.md</verify>
  <done>README and SPEC updated with Phase 7 features. TIMELINE_DATA marked complete.</done>
</task>

## Success Criteria
- [ ] `refreshGanttView` auto-syncs DASHBOARD
- [ ] README documents all new sheets and menu items
- [ ] SPEC.md updated with Phase 7 criteria
