# Plan 7.1 Summary: DASHBOARD Sheet & KPI Engine

## Changes
- **Config.gs**: Added `SHEET_DASHBOARD`.
- **Main.gs**: 
    - Updated `initStructure` to create and protect the `DASHBOARD` sheet.
    - Added "Refrescar Dashboard" to the menu.
- **Gantt.gs**: 
    - Implemented `refreshDashboard()` which calculates:
        - Global KPIs (Total Projects, Tasks, Completed, % General, Overdue).
        - Status Breakdown (counts per status).
        - Per-Project Summary (Tasks, Completed, %, Overdue, Status, Owner).
    - Added basic bold formatting for headers.

## Verification Results
- `initStructure` creates the sheet.
- `refreshDashboard` correctly counts tasks across different statuses and projects.
- Overdue logic correctly identifies tasks where `Fin < today` and not `Terminado/Cancelado`.
