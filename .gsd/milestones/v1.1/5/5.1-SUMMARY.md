# Summary 5.1: Dropdowns & ISSUES Sheet

## Accomplishments
- Added `SHEET_ISSUES` and `HEADERS_ISSUES` to `Config.gs`.
- Updated `initStructure` in `Main.gs`:
    - Creates the `ISSUES` sheet with headers.
    - Applies `VALID_STATUSES` dropdown to `PROJECTS.Estado`.
    - Applies dynamic dropdown to `TASKS.Proyecto` referencing `PROJECTS.Nombre`.
- Verified dropdowns and sheet creation via code logic.

## Verification
- Sheet constants exist in `Config.gs`.
- `Main.gs` logic uses `requireValueInList` and `requireValueInRange`.
