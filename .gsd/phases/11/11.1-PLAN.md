---
phase: 11
plan: 1
wave: 1
gap_closure: true
---

# Fix: Code Bugs & Naming

## Problem
Audit found: (1) `Notifications.gs` date format uses lowercase `mm` (minutes) instead of `MM` (months), (2) `Calendar.gs` name is confusing vs `CalendarSync.gs`.

## Root Cause
Date formatting pattern copied without checking GAS `Utilities.formatDate` spec. Calendar file predates the Google Calendar integration.

## Tasks

### Task 1: Fix date format in Notifications.gs
- **File**: `Notifications.gs`
- **Action**: Replace `'dd/mm/yyyy'` with `'dd/MM/yyyy'` in `buildAlertHtml_` (2 occurrences).
- **Verify**: Search for `dd/mm/yyyy` in codebase — should return 0 results.
- **Done**: All date formats use uppercase `MM`.

### Task 2: Rename Calendar.gs → CalendarGrid.gs
- **File**: `Calendar.gs` → `CalendarGrid.gs`
- **Action**: `git mv Calendar.gs CalendarGrid.gs`. No internal changes needed (function name `buildCalendarFromConfig` is unambiguous).
- **Verify**: `Calendar.gs` no longer exists; `CalendarGrid.gs` contains `buildCalendarFromConfig`.
- **Done**: File renamed, git history preserved.

### Task 3: Add SPEC.md success criterion for Suite Google
- **File**: `SPEC.md`
- **Action**: Add `- [x] Google Suite integrations (Calendar sync, Drive folders, Gmail alerts).` to Success Criteria.
- **Verify**: SPEC.md contains the new criterion.
- **Done**: SPEC updated.
