# Plan 11.1 Summary: Code Fixes & Naming

## Changes
- **Notifications.gs**: Fixed date format bug (`dd/mm` -> `dd/MM`) in HTML alerts.
- **File Rename**: `Calendar.gs` -> `CalendarGrid.gs` (Disambiguation).
- **SPEC.md**: Added success criterion for Google Suite integrations.

## Verification
- Code grep for `dd/mm/yyyy` returns 0 results.
- `CalendarGrid.gs` exists and contains `buildCalendarFromConfig`.
- SPEC.md reflects current features.
