# Plan 9.2 Summary: AppSheet Action Columns & Triggers

## Changes
- **Config.gs**:
    - Added `_Acción` column to `HEADERS_TASKS`.
- **Main.gs**:
    - `onEditDispatcher_(e)`: Intercepts edits to the `_Acción` column and dispatches to script functions (`refreshGanttView`, `autoUpdateProjectStatuses_`, `refreshDashboard`).
    - `setupAppSheetTrigger()`: Menu-accessible function to install the required `onEdit` trigger for the dispatcher.
    - `initStructure()`: Added data validation (dropdown) for the `_Acción` column.
    - Added menu item "⚡ Configurar trigger AppSheet".

## Verification Results
- `_Acción` column successfully added to TASKS.
- Validation dropdown restricted to 'Refrescar', 'Estados', 'Dashboard'.
- Dispatcher correctly clears the action cell after execution to prevent loops.
